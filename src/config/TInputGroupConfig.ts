const TInputGroupConfig = {
  classes: {
    wrapper: '',
    label: 'block',
    body: '',
    feedback: 'text-gray-400 text-sm',
    description: 'text-gray-400 text-sm',
  },
};

export type TInputGroupConfigKeys = 'wrapper' | 'label' | 'body' | 'feedback' | 'description';

export default TInputGroupConfig;
